# Bot token, generated on Discord's developer's portal in "Bot" tab after
# selecting the app and setting it as a bot account
TOKEN = ''

# ID of the message people are supposed to react to
REACTION_MESSAGE_ID = 0

# ID of the channel that message is on
REACTION_CHANNEL_ID = 0

# a Python dictionary containing emojis paired with corresponding roles
# the key (left value) should be the emote itself, unicode value, not
# the colon coding (😮 =/= :open_mouth:), unless the emoji is custom,
# then you should use its name WITHOUT the colons
REACTION_MATCH = {
    "😮": "some_role",
}
